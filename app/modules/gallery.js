define([], function() {
  'use strict';

  function Gallery() {
    var self = this;

    self.uploadImage = function(image) {
      // Code here to upload the image
    };

    self.deleteImage = function(image) {
      // Code here to delete image
    };
  }

  return Gallery;

});
